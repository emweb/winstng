diff -ru /cygdrive/c/tmp/mariadb-native-client/CMakeLists.txt mariadb-src/CMakeLists.txt
--- /cygdrive/c/tmp/mariadb-native-client/CMakeLists.txt	2012-11-29 04:29:20.000000000 +0100
+++ mariadb-src/CMakeLists.txt	2013-09-19 15:13:21.735502500 +0200
@@ -14,6 +14,8 @@
   CMAKE_POLICY(SET CMP0003 NEW)
 ENDIF(COMMAND CMAKE_POLICY)
 
+SET(DEBUG_LIB_POSTFIX "d" CACHE STRING "String appended to debug libraries")
+
 IF(WIN32)
   # Speedup system tests
   INCLUDE(${CMAKE_SOURCE_DIR}/WindowsCache.cmake)
@@ -111,12 +113,6 @@
 ENDIF()
 
 
-IF(WIN32)
- SET(CMAKE_INSTALL_PREFIX "")
-ENDIF()
-
-
-
 SET(CPACK_PACKAGE_VENDOR "Monty Program AB")
 SET(CPACK_PACKAGE_DESCRIPTION "MariaDB client library. A library for connecting to MariaDB and MySQL servers")
 SET(CPACK_PACKAGE_NAME "mariadb_client")
Only in mariadb-src: CMakeLists.txt~
diff -ru /cygdrive/c/tmp/mariadb-native-client/libmysql/CMakeLists.txt mariadb-src/libmysql/CMakeLists.txt
--- /cygdrive/c/tmp/mariadb-native-client/libmysql/CMakeLists.txt	2012-11-29 04:29:20.000000000 +0100
+++ mariadb-src/libmysql/CMakeLists.txt	2013-09-19 15:12:30.310561100 +0200
@@ -132,6 +132,9 @@
 IF(OPENSSL_FOUND)
   TARGET_LINK_LIBRARIES(mariadbclient ${OPENSSL_LIBRARIES})
 ENDIF()
+SET_TARGET_PROPERTIES(mariadbclient PROPERTIES
+   DEBUG_POSTFIX ${DEBUG_LIB_POSTFIX}
+)
 
 ADD_LIBRARY(libmariadb SHARED ${LIBMYSQL_SOURCES})
 TARGET_LINK_LIBRARIES(libmariadb ${CMAKE_THREAD_LIBS_INIT} ${CMAKE_DL_LIBS})
@@ -156,9 +159,12 @@
 
 SET_TARGET_PROPERTIES(libmariadb PROPERTIES PREFIX "")
 
-SET_TARGET_PROPERTIES(libmariadb PROPERTIES VERSION 
-   ${CPACK_PACKAGE_VERSION_MAJOR}
-   SOVERSION ${CPACK_PACKAGE_VERSION_MAJOR})
+SET_TARGET_PROPERTIES(libmariadb PROPERTIES
+   VERSION ${CPACK_PACKAGE_VERSION_MAJOR}
+   SOVERSION ${CPACK_PACKAGE_VERSION_MAJOR}
+   PREFIX ""
+   DEBUG_POSTFIX ${DEBUG_LIB_POSTFIX}
+)
 
 #
 # Installation
@@ -166,7 +172,7 @@
 
 INSTALL(TARGETS
           libmariadb mariadbclient
-          RUNTIME DESTINATION "lib"
+          RUNTIME DESTINATION "bin"
           LIBRARY DESTINATION "lib"
           ARCHIVE DESTINATION "lib")
 
Only in mariadb-src/libmysql: CMakeLists.txt~
